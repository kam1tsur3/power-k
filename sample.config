# sample config file

# Specify directories that are skipped in elf and func modes
[directory]
./root/sbin
./root/bin

# Specify file extensions that are skipped in elf and func modes
[extension]
so

# Specify file regex that are skipped in elf and func modes
[regex]
\.so\..*
